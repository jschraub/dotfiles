[terminal]
# The VT to run the greeter on. Can be "next", "current" or a number
# designating the VT.
vt = 1

[default_session]
# `agreety` is the default greeter; we replace it with Hyprland + ReGreet.
command = "dbus-run-session start-hyprland -- -c /etc/greetd/hyprland.conf"
user = "greeter"
